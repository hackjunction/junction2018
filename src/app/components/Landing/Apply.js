import React from 'react';
import { Row, Col } from 'react-flexbox-grid';
import Scroll from 'react-scroll';

const Apply = () => <Scroll.Element name="apply">Apply</Scroll.Element>;

export default Apply;
