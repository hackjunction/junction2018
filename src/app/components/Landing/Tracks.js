import React from 'react';
import { Row, Col } from 'react-flexbox-grid';
import Scroll from 'react-scroll';

const Tracks = () => <Scroll.Element name="tracks">Tracks</Scroll.Element>;

export default Tracks;
