import React from 'react';
import { Row, Col } from 'react-flexbox-grid';
import Scroll from 'react-scroll';

const PreEvents = () => <Scroll.Element name="preevents">PreEvents</Scroll.Element>;

export default PreEvents;
