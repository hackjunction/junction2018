import React from 'react';
import { Row, Col } from 'react-flexbox-grid';
import Scroll from 'react-scroll';

const Hero = () => <Scroll.Element name="hero">Hero</Scroll.Element>;

export default Hero;
